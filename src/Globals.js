const Globals = {}

Globals.CIRCLE = Math.PI * 2
Globals.PI_HALF = Math.PI / 2
Globals.PI_QUARTER = Math.PI / 4

Globals.canvasOptions = {
  alpha: false,
  desynchronized: true
}

export default Globals
